# Not needed for local dev: requests default to a relative /api/v1 path, proxied to the
# backend by vite.config.ts (keeps everything same-origin so the httpOnly refresh cookie
# works correctly). Only set this to point at a different backend origin.
# VITE_API_URL="http://localhost:5000/api/v1"
